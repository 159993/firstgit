server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boardgame?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: root
    password: a123456
  thymeleaf:
    prefix: classpath:/templates/
mybatis-plus:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  type-aliases-package: com.zzti.pojo