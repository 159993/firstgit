<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzti.mapper.EquipmentMapper">
    <!--留着以后用  -->
    <select id="selectList" parameterType="java.util.HashMap" resultType="com.zzti.pojo.Equipment">

        SELECT * FROM equipment
        <where>
            <if test="equipmentName != null">
                name LIKE concat('%',#{equipmentName},'%')
            </if>
        </where>
        <if test="page != null and limit !=null ">
            LIMIT #{page},#{limit}
        </if>

    </select>

</mapper>